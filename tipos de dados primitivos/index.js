const nome = 'Gabryel'; //string
const nome2 = "Gabryel"; //string
const nome3 = `Gabryel`; //string
const num1 = 10;  //number
const num12 = 10.3;  //number
let nomeAluno; //undeined = não aponta pra local nenhum na memória
const sobrenomeAluno = null; // Nulo = não aponta pra local nenhum na memória
const aprovado = true; // boolean = true, false (Lógico)

